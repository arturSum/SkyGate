
 var currentClickedButton = event.target;

        if(lastClickedButton === currentClickedButton){
            return;
        }



        lastClickedButton.className = lastClickedButton.replace('active', '');

        currentClickedButton.className = `${currentClickedButton.className} active`;

        lastClickedButton = currentClickedButton;